#include<cs50.h>
#include<stdio.h>
#include<ctype.h>

int count_letters(string text);

int main(void)
{
    string text = get_string("Text: ");
    printf("%s", text);
    int count_letters(text)
}

int count_letters(string text)
{
    int letters = 0;
    for( i = 0; text != 0; i++)
    {
         if(isalpha(text) != 0)
         {
             letters++;
         }
    }

}